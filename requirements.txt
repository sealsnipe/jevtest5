requests>=2.31
# Relay (relay/app.py)
fastapi>=0.115
uvicorn>=0.30
python-dotenv>=1.0
httpx>=0.27          # nur für fastapi.testclient (tests/relay_test.py)
# Lokale Transkription im Relay (relay/stt.py), siehe docs/stt_setup.md
onnx-asr[gpu,hub]>=0.12   # CPU-only: onnx-asr[cpu,hub]
# CUDA-13-Laufzeit für onnxruntime-gpu (Windows-Wheels): nvidia-cublas nvidia-cuda-runtime nvidia-cudnn-cu13 nvidia-cufft nvidia-curand nvidia-cuda-nvrtc
